# Contributing

Keep changes small, idiomatic, and covered by tests. Run `make test vet ios` before opening a pull request. Architecture changes should include or update an ADR. Platform code must retain real native controls, batch bridge traffic, and enforce UI-thread mutation.
